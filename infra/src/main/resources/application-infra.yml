spring:
  config:
    activate:
      on-profile: local
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:dldb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
external:
  github:
    url: https://github.com/login/oauth/access_token
    headers:
      #      client-id: ${GITHUB_API_CLIENT_ID}
      #      client-secret: ${GITHUB_API_CLIENT_SECRET}
      client-id: Ov23li4nGa9BQUD7jOUz
      client-secret: 1359bb8d6b4cc975f069794e22b06d7e2d5ac099
